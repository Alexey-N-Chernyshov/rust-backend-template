#[derive(Clone)]
pub struct ServerConfig {
    // TODO set up config
}

impl ServerConfig {
    pub fn new_from_envs() -> Self {
        Self {
            // TODO read config from envs
        }
    }
}
